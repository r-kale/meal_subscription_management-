// ── App configuration ─────────────────────────────────────────────
// Fill in your Supabase project's URL and anon (public) key, then
// commit this file. See docs/SETUP.md for step-by-step instructions.
//
// Until both values are filled in, the app runs in Demo Mode only
// (data stays in the browser).
window.APP_CONFIG = {
  supabaseUrl: '',      // e.g. 'https://abcdefgh.supabase.co'
  supabaseAnonKey: '',  // Settings → API → anon public key
};
